import AllProjects from "@/components/projects/AllProjects";

/**
 * ProjectsPage - Page listant tous les projets
 * @returns {JSX.Element} Composant affichant tous les projets
 * @description Affiche la grille de tous les projets avec animation de reveal au scroll
 * @route /projects
 */
export default function ProjectsPage() {
    return <AllProjects />;
}
